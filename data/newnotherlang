@start
	object k1 Kappa $config
	move k1 655 505
	set _k1.animation left
	
	if k1.animation == "left" then
	biba = "hoho"
	set isi 500
	biba += isi //"hoho500"

	a : (biba += isi)
	b = 775
	b += "600"
/start



b = 700
c = (b + a)
c : (b + a)

dc : c + " - debil"
ddc : dc + " , " + dc

ddc : "lolka"

a = ddc + "123"

c: a or b